app:
  name: demoBatch

spring:
  application:
    name: ${app.name}
  datasource:
    url: ${DATASOURCE_URL:jdbc:h2:./data/testdb}
    driver-class-name: org.h2.Driver
    username: ${DATASOURCE_USERNAME:sa}
    password: ${DATASOURCE_PASSWORD:teste}
  h2:
    console:
      enabled: true
      path: /h2-consol
